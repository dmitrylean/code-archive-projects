---
title: "Импорт JSX и TSX-компонентов в MDX."
category: "Astro"
---

> `Cannot find module 'react' imported from ...`
> означает, что Astro не умеет обрабатывать React-компоненты — потому что React пока **не подключён в проект**.

---

### 🧭 Как это починить — пошагово

#### 1. Установи нужные пакеты

```bash
npm install react react-dom @astrojs/react @astrojs/mdx
```

---

#### 2. Обнови `astro.config.mjs`

Добавь интеграции React и MDX:

```js
import { defineConfig } from "astro/config";
import react from "@astrojs/react";
import mdx from "@astrojs/mdx";

export default defineConfig({
  integrations: [react(), mdx()],
});
```

Это обязательно — без этого Astro не сможет связать MDX и JSX.

---

#### 3. Проверь импорт компонента

Путь должен быть **относительно MDX-файла** или через алиас `@/components`, если он настроен в `tsconfig.json` / `jsconfig.json`.

Например:

```mdx
---
title: "React-компоненты"
image: "https://decode.kz/images/blog/5f9d437783069466b99dfd30.png"
---

import AlbumCard from "@/components/jsx/AlbumCard.jsx";

<AlbumCard />
```

---

#### 4. Если алиас `@` не работает

Проверь наличие `jsconfig.json` в корне проекта:

```json
{
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@/*": ["src/*"]
    }
  }
}
```

---

#### 5. Перезапусти Astro

```bash
npm run dev
```

После этого `MDX` начнёт понимать JSX-компоненты и рендерить их как положено.
А ошибка `Cannot find module 'react'` исчезнет.

---

### 🚀 Как исправить ошибку `Cannot find module '@/components/...’`

Есть три способа — я покажу от самого простого к более продвинутому 👇

---

## ✅ Способ 1 — Прямой относительный путь

Проверь структуру проекта и укажи путь **от MDX-файла до компонента**:

Пример:

```
src/
 ├─ components/
 │   └─ jsx/
 │       └─ AlbumCard.jsx
 └─ content/
     └─ example/
         └─ jsxcomponents.mdx
```

Тогда в `.mdx` напиши так:

```mdx
import AlbumCard from "../../components/jsx/AlbumCard.jsx";

<AlbumCard />
```

⚠️ Важно — **укажи расширение `.jsx`**, иначе Astro не найдет файл при сборке.

---

## ✅ Способ 2 — Настроить алиас `@`

Создай (или проверь) файл `jsconfig.json` или `tsconfig.json` в корне проекта:

```json
{
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@/*": ["src/*"]
    }
  }
}
```

А в `astro.config.mjs` добавь:

```js
import { defineConfig } from "astro/config";
import react from "@astrojs/react";
import mdx from "@astrojs/mdx";

export default defineConfig({
  integrations: [react(), mdx()],
  vite: {
    resolve: {
      alias: {
        "@": "/src",
      },
    },
  },
});
```

Теперь можно писать:

```mdx
import AlbumCard from "@/components/jsx/AlbumCard.jsx";
```

---

## ✅ Способ 3 — Проверить регистр и расширение

Иногда на Windows всё работает, а при деплое на Linux ломается из-за регистра букв.
Проверь:

* Папка называется **jsx**, а не `JSX`
* Файл действительно **AlbumCard.jsx**
* В импорте **есть** `.jsx`

---

### ⚡ Финальный чек

После фикса:

1. Перезапусти Astro → `npm run dev`
2. Убедись, что в терминале нет ошибки `Cannot find module`
3. MDX должен отрендерить `<AlbumCard />` прямо внутри статьи 🎶

---